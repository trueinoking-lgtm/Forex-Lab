ENGINE_DIR=../engine
# Optional market-data API key (never required; engine works key-less via yfinance).
# If you add a paid FX adapter later, set the key here and it is redacted in logs/UI.
YFINANCE_API_KEY=
# paper-only safety — must stay false for live orders
ALLOW_LIVE_ORDERS=false
