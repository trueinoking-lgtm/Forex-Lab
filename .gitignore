# Python
engine/.venv/
engine/__pycache__/
engine/*.pyc
__pycache__/

# Node
node_modules/
.next/
.next_old_*/
out/

# Engine generated outputs (regenerated by backtest/signals/import runs)
engine/results/
engine/data/yf_*.csv
engine/data/canonical_*.csv
engine/data/raw_*.csv
engine/data/*.manifest.json
engine/data/imports.json

# Local data / runtime
data/
app/forex_lab.db
app/forex_lab.db-shm
app/forex_lab.db-wal
*.log
.DS_Store

# Secrets (never commit)
.env
.env.local
*.env
tsconfig.tsbuildinfo

# Kronos Phase 0: evidence excluded from VCS, kept on disk only
# engine/evidence/kronos/phase0/ contains hashes and manifests only
